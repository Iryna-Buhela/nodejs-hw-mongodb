export async function initMongoConnection(params) {
  MONGODB_USER;
  MONGODB_PASSWORD;
  MONGODB_URL;
  MONGODB_DB;
  console.log('Mongo connection successfully established!');
}

initMongoConnection();
